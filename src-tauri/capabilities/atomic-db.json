{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "atomic-db",
  "description": "Capability for atomic database window",
  "windows": ["atomic-db-window"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-emit",
    "core:window:allow-close"
  ]
}
